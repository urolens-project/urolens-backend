"""FastAPI routers for the core (non-intake/request-domain) API surface: auth,
patients, images, results, queue, notifications, sync, and the physician/
patient-portal routes. No public re-exports — import routers directly and
mount them in `main.py`.
"""

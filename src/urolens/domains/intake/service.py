"""Empty file with no importers anywhere in the repo (grep-verified) — a
rule-14 dead-file candidate, not deleted here since this pass is
documentation-only. See the flagged-findings changelog entry."""

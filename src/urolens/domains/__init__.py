"""Domain-specific routers not yet folded into `api/`: specimen intake
(`intake/`) and lab-request creation (`request/`). No public re-exports —
import routers directly and mount them in `main.py`."""

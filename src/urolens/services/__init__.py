"""Business-logic layer: one module per domain service, called from
`api/`/`domains/` routers. No public re-exports — import services directly
(`from src.urolens.services.patient_service import PatientService`, etc.)."""
